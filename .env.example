# OpenRouteService — routing com avoid_polygons (servidor apenas, não expor no cliente)
ORS_API_KEY=sua_chave_aqui

# Postgres — capacidade dos abrigos em tempo real (opcional; sem isto usa dados estáticos)
DATABASE_URL=postgres://utilizador:senha@host:5432/base

# OpenAI — assistente USSD/SMS de linguagem natural (servidor apenas)
OPENAI_API_KEY=sua_chave_aqui

# PIN de 4 dígitos para o modo staff
STAFF_PIN=1234
